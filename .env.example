# Copy this to ".env" (which is git-ignored) and fill in your values.
# Load it into your shell before running, with:
#     set -a; source .env; set +a
#
# Schwab access comes from the central schwab_hub (../schwab_hub/run.sh); no credentials here.
# SCHWAB_HUB_URL=http://127.0.0.1:8765
#
