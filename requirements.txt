# gex.py core dependencies. Python 3.11+ recommended (core code is 3.9+ compatible).
numpy>=1.24
matplotlib>=3.6
pytest>=7.0

# Live data source: Charles Schwab Trader API, served by the local schwab_hub
# (../schwab_hub; run it with ../schwab_hub/run.sh). This installs its client package.
# The hub needs Python >= 3.10; the marker makes `pip install -r` skip it on 3.9, where
# --demo and the unit tests still run.
-e ../schwab_hub ; python_version >= "3.10"
